# PINTAO — Legacy (archivada)

Estos archivos son la **tienda anterior** (HTML/CSS/JS plano + Node `.js`) que estuvo desplegada en Vercel. Se archivan como **referencia histórica**; todo el trabajo activo vive en [`web/`](../web/README.md)(Next.js 15).

**No modificar esta carpeta** — los cambios van en `web/`.